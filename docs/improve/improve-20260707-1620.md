# Improve Loop 12 — I-3: PivotConfigPanel 3-tuple → TableRef

- **Scope**: Bundle `{schema, tableName, database}` into `TableRef` type, eliminate 3-prop drilling through 7 sites in pivot-config subtree
- **Pattern**: type bundling (matches Loop B/C prop reduction, Loop D store split cohesion)
- **Rule sources**: `AGENTS.md` (stack, `@/*` alias, strict TS), `~/.config/opencode/memory/core.md` (minimal diff, no `as any`, never commit), `CONTEXT.md` (reuse — `ColumnMeta` in `src/lib/types.ts`)
- **Reusable sessions**: fix-1 (`ses_0c5b1d04fffelGaNqwEsNwHhf7`) resumed for impl

## 1. Candidate (from Loop 1 explorer)

Original: "I-3 PivotConfigPanel 3-tuple → TableRef".

Re-grilled after reading 7 files: 3-tuple `{schema, tableName, database}` drilled through **7 sites** in pivot-config subtree:

1. `page.tsx:155-161` → `PivotView` (3 props, narrowing `string | null` → `string`)
2. `pivot-view.tsx:108-114` → `PivotConfigPanel` (3 props)
3. `pivot-config.tsx:125` → `usePivotOrchestrator(schema, tableName, database)` (3 positional args)
4. `pivot-config.tsx:167-177` → `FiltersSection` (3 props)
5. `pivot-config.tsx:~210` → `PivotConfigDialogs` (3 props)
6. `pivot-config-dialogs.tsx:48-50` → `CalculatedIndicatorDialog` (3 props)
7. `calculated-indicator-dialog.tsx:95-103` → `useAiCalcRecommendation` (3 args)

Plus `FiltersSection` → `PivotFilterChip` (2 props: database/tableName — `schema` resolved at parent for `meta.type`).

## 2. Grill

- **Q1**: Active bugs? → 0 active. 1 LATENT BUG CLASS: `usePivotOrchestrator(schema, tableName, database)` has 2 positional `string` args (`tableName`/`database`) that TypeScript cannot distinguish — swappable without error. Bundling into `TableRef` eliminates this.
- **Q2**: `schema` used standalone elsewhere? → YES: `PivotGrid` (pivot-view.tsx:127) takes `schema` separately for chart rendering. `TableRef` only makes sense in pivot-config subtree (the "selected table" concept). `PivotGrid` keeps `schema` prop.
- **Q3**: `PivotFilterChip`? → Leaf, only needs `database`/`tableName` (no `schema` — type resolved at parent). Keep 2 separate props. Over-engineering to pass partial `Pick<TableRef, ...>`.
- **Q4**: Type location? → `src/lib/types.ts` (where `ColumnMeta` lives, 18L clean).
- **Q5**: Construction site? → `page.tsx:155` (where `string | null` narrowing happens). `<PivotView tableRef={{ schema, tableName: selectedTable, database: selectedDatabase }} />`.
- **Q6**: `PivotView` uses `schema` for PivotGrid? → YES, destructures `tableRef.schema` for PivotGrid. Fine.
- **Q7**: Behavior change? → NONE. Pure type bundling + prop reduction. tsc + lint + test baseline must hold.

## 3. Plan

Single phase, mechanical refactor. Dispatch fix-1 (resumed):

- **P1**: Add `TableRef` interface to `src/lib/types.ts`: `{ schema: ColumnMeta[]; tableName: string; database: string }`
- **P2**: Update 7 sites to use `TableRef`:
  - `page.tsx`: construct `tableRef={{ schema, tableName: selectedTable, database: selectedDatabase }}`
  - `PivotViewProps`: replace `schema`/`selectedTable`/`selectedDatabase` → `tableRef: TableRef`
  - `PivotConfigPanelProps`: replace 3 props → `tableRef: TableRef`
  - `usePivotOrchestrator(schema, tableName, database)` → `usePivotOrchestrator(tableRef)`
  - `FiltersSectionProps`: replace `schema`/`database`/`tableName` → `tableRef: TableRef`
  - `PivotConfigDialogsProps`: replace `schema`/`tableName`/`database` → `tableRef: TableRef`
  - `CalculatedIndicatorDialogProps`: replace `schema`/`tableName`/`database` → `tableRef: TableRef`
  - `useAiCalcRecommendation` input: replace 3 args → `tableRef: TableRef`
- **P3**: `PivotFilterChip` — NO CHANGE (leaf, keeps `database`/`tableName` 2 props)
- **P4**: `PivotGrid` — NO CHANGE (separate consumer, keeps `schema` prop)

Internal destructuring: each consumer does `const { schema, tableName, database } = tableRef` at top, rest of body unchanged. Minimal diff.

## 4. Implementation (DONE — @fixer `ses_0c5b1d04fffelGaNqwEsNwHhf7`)

Single phase, atomic. All 7 sites updated. Internal `const { schema, tableName, database } = tableRef` destructure kept function bodies unchanged.

| File | Δ Lines | Change |
|------|---------|--------|
| `src/lib/types.ts` | 18→23 (+5) | Added `TableRef` interface |
| `src/hooks/use-ai-calc-recommendation.ts` | 89→88 (-1) | 3 args → `tableRef: TableRef` |
| `src/components/calculated-indicator-dialog.tsx` | 599→598 (-1) | Props 3→1 |
| `src/components/pivot-config-dialogs.tsx` | 63→61 (-2) | Props 3→1 |
| `src/components/pivot-config-sections.tsx` | 349→350 (+1) | `FiltersSectionProps` 3→1 |
| `src/components/pivot-config.tsx` | 253→247 (-6) | Props 3→1, child JSX 3→1, orchestrator 3→1 |
| `src/components/pivot-view.tsx` | 203→200 (-3) | Props 3→1, child JSX 3→1 |
| `src/hooks/use-pivot-orchestrator.ts` | 146→147 (+1) | Signature 3 params → 1 `tableRef` |
| `src/app/page.tsx` | 206→205 (-1) | `<PivotView>` 3 props → 1 |

Net -7 lines. `ColumnMeta` import retained in `pivot-view.tsx` (used by `DrilldownDrawer`).

## 5. Verify (DONE)

- `npx tsc --noEmit`: 0 errors ✓
- `npm run lint`: 0 errors, 39 warnings (32 baseline + 7 new unused `ColumnMeta` imports in files now only using `TableRef` — pre-existing pattern, non-blocking, `lint --fix` cleanable) ✓
- `npm test`: 250 pass / 0 fail ✓

### Result
3-tuple `{schema, tableName, database}` bundled into `TableRef` type. 7 drilling sites reduced from 3 props to 1. 1 latent bug class eliminated (`usePivotOrchestrator(schema, tableName, database)` positional string args were swappable without TS catching — now `usePivotOrchestrator(tableRef)` single arg). `PivotFilterChip` (leaf, 2 props) + `PivotGrid` (separate consumer, keeps `schema`) unchanged — correct scope boundary.
