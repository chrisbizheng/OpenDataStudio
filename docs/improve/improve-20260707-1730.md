# Improve Loop 16 — M-2: calculated-indicator-dialog.tsx i18n leak + _t prop drilling

- **Scope**: `src/components/calculated-indicator-dialog.tsx` (594L) — 1 visible i18n leak + 2 subcomponents `_t` prop drilling
- **Pattern**: Loop 13/14/15 i18n leak + Cluster C-hooks `_t` prop elimination
- **Rule sources**: `AGENTS.md` (i18n `useLang()`), `~/.config/opencode/memory/core.md` (minimal diff)
- **Reusable sessions**: none dispatched (mechanical direct edit)

## 1. Candidates

M-2 from Loop 1 explorer: "`calculated-indicator-dialog.tsx` 599L — large but cohesive form/dialog".

Re-grill after reading (594L): file is genuinely cohesive (expression editor domain: AST logic + text input + field/function panels + AI recommendation + format config). NO extraction value (M-4/M-6 pattern — subcomponents already extracted: `FieldPanel` L483-560, `FunctionPanel` L562-594).

**Real bugs**:
1. **i18n leak (HIGH)**: L573 visible label `函数` hardcoded Chinese. EN mode shows "函数" instead of "Functions".
2. **`_t` prop drilling (MED)**: `FieldPanel` + `FunctionPanel` receive `_t` as prop (L390, L420). Same pattern as Cluster C-hooks — could self-subscribe `useLang()`. Not in original Cluster C scope (that was agent-chat subtree; this is pivot-config subtree).

**SKIP**:
- aria-labels hardcoded Chinese (~10): L310/321/394/407/505/514/522/530/549/572/582. Accessibility-only, EN screen readers read Chinese. 10 new i18n keys = high churn for low impact. DEFERRED.
- File extraction: already cohesive, subcomponents already extracted.
- `inferVizConfig`-style pure fn extraction: `createEmptyLogic`/`generateCalcKey` are domain helpers, 1 consumer each, cohesion argument.

## 2. Grill

- **Q1**: i18n key name for `函数`? → `calc_ind.functions` = {zh:"函数", en:"Functions"}. Consistent with `calc_ind.available_fields`/`calc_ind.all_fields` pattern.
- **Q2**: Self-subscribe pattern safe? → YES. `FieldPanel`/`FunctionPanel` are internal subcomponents (not exported, only used at L378/L415). Adding `const { _t } = useLang()` at top, dropping `_t` from props interface. Same as Cluster C 11 leaf components.
- **Q3**: Other props to eliminate? → NO. `FieldPanel` receives 9 domain props (schema/availableIndicators/etc.) — all genuine data. `FunctionPanel` receives 4 domain props — all genuine. Only `_t` is the i18n prop that can be self-subscribed.

## 3. Plan

Single phase, direct edit (1 file + 1 i18n key, ~15 lines diff):

- Add `calc_ind.functions` to `src/lib/i18n.ts`
- L573: `函数` → `{_t("calc_ind.functions")}`
- `FieldPanel`: add `const { _t } = useLang()` at top, drop `_t` from props interface, drop `_t` from call site (L390)
- `FunctionPanel`: same pattern, drop `_t` from call site (L420)

## 4. Implementation (pending — direct edit)

## 5. Verify (pending)

- `npx tsc --noEmit`: 0 errors
- `npm run lint`: 0 errors (≤37 warnings baseline)
- `npm test`: 250 pass / 0 fail
