# Improve Loop 15 — M-3: create-widget-sql-dialog.tsx i18n leak

- **Scope**: `src/components/create-widget-sql-dialog.tsx` (393L) — 2 i18n leak bugs (English hardcoded)
- **Pattern**: Loop 13/14 i18n leak pattern, reversed (English hardcoded instead of Chinese)
- **Rule sources**: `AGENTS.md` (i18n custom `useLang()` + dict), `~/.config/opencode/memory/core.md` (minimal diff, no `as any`, never commit)
- **Reusable sessions**: none dispatched (small direct edit)

## 1. Candidates

M-3 from Loop 1 explorer: "`create-widget-sql-dialog.tsx` 383L — dashboard widget form, large but cohesive".

Re-grill after reading (393L post-Loop 9+11):
- **i18n leak 1 (HIGH)**: L183 `({queryResult.rows.length} rows)` — hardcoded English "rows". ZH mode shows "N rows" instead of "N 行".
- **i18n leak 2 (HIGH)**: L216 `Showing first 50 of {queryResult.rows.length} rows` — hardcoded English sentence. ZH mode shows English.
- **DRY candidate SKIP**: X axis Select (L257-271) + Y axis Select (L277-291) near-identical. Only 2 instances — below DRY threshold (Loop A extracted PivotFieldSelect for 4 instances). 2 instances = single-call-site subcomponent risk.
- **inferVizConfig extraction SKIP**: L30-49 pure function, 1 consumer. Cohesion argument: stays with sole consumer.
- **VizConfig form extraction SKIP**: L227-378 (152L) large but 1 consumer, cohesive form. M-4/M-6 pattern — extraction = negative value.
- **Query result table extraction SKIP**: L178-225 (48L), 1 consumer. Same pattern.

## 2. Grill

- **Q1**: Why reuse `grid.rows` instead of new `dashboard.rows`? → 5 existing keys (`schema.rows`/`grid.rows`/`agent.rows`/`history.rows`/`pivot.result_rows`) all = {zh:"行", en:"rows"}. Adding 6th = more duplication. `grid.rows` semantically closest (data grid row count). REUSE.
- **Q2**: L216 sentence — can split-concatenate work? → ZH needs different word order ("显示前 50 条，共 N 行" vs en "Showing first 50 of N rows"). Need 1 new key for the sentence prefix + reuse `grid.rows` for unit.
- **Q3**: New key name? → `dashboard.showing_first_of` = {zh:"显示前 50 条，共", en:"Showing first 50 of"}. Then `{_t("dashboard.showing_first_of")} {queryResult.rows.length} {_t("grid.rows")}`.
- **Q4**: Other hardcoded English in file? → L150 spinner `▶` (symbol, not text). L204 `null` (code literal, not UI text). L322 placeholder logic (already i18n). L362 `✕` (symbol). No other leaks.

## 3. Plan

Single phase, direct edit (no specialist — <10 lines, 1 file + 1 i18n key):

- Add `dashboard.showing_first_of` to `src/lib/i18n.ts`
- L183: `({queryResult.rows.length} rows)` → `({queryResult.rows.length} {_t("grid.rows")})`
- L216: `Showing first 50 of {queryResult.rows.length} rows` → `{_t("dashboard.showing_first_of")} {queryResult.rows.length} {_t("grid.rows")}`

## 4. Implementation (pending — direct edit)

## 5. Verify (pending)

- `npx tsc --noEmit`: 0 errors
- `npm run lint`: 0 errors (≤37 warnings baseline)
- `npm test`: 250 pass / 0 fail (baseline preserved — no behavior change to tested paths)
