# Improve Loop — Frontend panels — 2026-07-07 09:48

## 0. Bootstrap
- **Scope**: Frontend panels (Sidebar / Center Grid+Pivot / Right AgentChat)
- **Rule sources loaded**:
  - `AGENTS.md`: `/Users/chrisbi/Documents/KELI/workspaces/OpenDataStudio/AGENTS.md`
  - `CLAUDE.md`: project root (`@AGENTS.md` re-export); `~/.config/opencode/memory/tech-stack.md` (stale — describes Vite/React18, project actually Next.js+React19; treat stack constraints from AGENTS.md as authoritative)
  - `core.md`: `~/.config/opencode/memory/core.md` (中文回复、strict TS、no `as any`/`@ts-ignore`、minimal diff、verify via tests/lsp)
  - `CONTEXT.md`: project root — rich domain glossary (Catalog / QueryEngine / QueryLifecycle / Pivot Execution Lifecycle / AgentChatSession / Column Type Classifier / Cell Renderer / chart-types hub / prompts dir)
  - `docs/adr/`: **absent** (no `docs/adr/` dir; decisions live in `issues/*.md` — 18 issue files)
- **Rule snapshot (hard constraints governing selection)**:
  - Every component uses `"use client"` — no server components
  - Path alias `@/*` → `./src/*`
  - shadcn/ui `base-nova` in `src/components/ui/`; style via Tailwind 4 + `cn()`
  - `@tanstack/react-table` NOT used (hand-rolled `<table>` + `@tanstack/react-virtual`); do not import for new features
  - `@tanstack/react-virtual` IS used
  - `sql-formatter` with `{ language: "clickhouse" }`
  - `src/stores/ui.ts`: `activeTab`/`setActiveTab` removed — right panel hard-wired to AgentChat; persisted legacy `"schema"` ignored on rehydrate
  - Custom `useLang()` + `src/lib/i18n.ts` dictionary (NOT next-intl)
  - Custom `ThemeProvider` (`resolved` light/dark for VTable/Chart keys)
  - No `typecheck`/`test` scripts — manual `npx tsc --noEmit`; no test framework configured
  - Read-only SQL enforcement in `/api/query`
  - Minimal diff, never leave broken state, never commit unless asked
  - CONTEXT.md deep modules already exist: Catalog, QueryEngine, QueryLifecycle, PivotExecutionLifecycle, AgentChatSession, ColumnTypeClassifier, CellRenderer — reuse these seams, do not reinvent
- **Explorer task id**: `ses_0c5bd0122ffezhQhtfgcnmFEmO` (alias `exp-1`)

## 1. Candidates

Explorer surfaced 38 numbered candidates across 7 axes. Full list preserved verbatim from explorer report (paths + line refs retained for grill). Clusters identified for grill efficiency:

### Cluster A — `pivot-config.tsx` god-component (M-1 + S-2 + S-6 + C-1 + X-6 + D-3 + S-1)
- 476 lines, mixes: 5 Sections + 2 Dialogs + DnD wrappers + 4 copy-pasted `Select` blocks + domain constants (`AGGREGATION_OPTIONS`/`formatLabel`) + cross-store write (`useDatasetStore.getState().selectTable` at l:384) + 5 useState (some single-use) + whole-object `usePivotStore()` subscription at l:82
- Files: `src/components/pivot-config.tsx`

### Cluster B — prop-drilling interfaces (I-1 + I-2 + S-5 + L-5 + I-4)
- `GridView` 16-prop (leaks query lifecycle internals: `sort`, `searchQuery`, `pendingAutoExecute`, `loadedRows`, 7 callbacks)
- `AssistantMessage` 14-prop (leaks LLM state: `messages` array, `aiFollowUpQuestions`, 4 callbacks)
- `useQueryOrchestrator` returns 16 flat fields destructured at `page.tsx:55-74` then re-fanned to views
- Files: `src/components/grid-view.tsx:13-32`, `src/components/agent-chat/assistant-message.tsx:20-36`, `src/app/page.tsx:55-74`

### Cluster C — `_t`/`lang` prop drilling (L-1)
- 11 agent-chat subtree components receive `_t` + `lang` as props despite `useLang()` context existing (`src/components/lang-provider.tsx`)
- Pure deletion-of-props leverage: remove 22 prop slots across 11 files, components self-subscribe
- Files: `src/components/agent-chat/{header,input-bar,initial-suggestions,thinking-panel,suggestion-list,message-actions,dashboard-selector-dialog,chart-detail-card,data-table,assistant-message}.tsx`, `src/components/agent-chat.tsx`

### Cluster D — `pivot.ts` single store three duties (D-1 + L-2)
- `usePivotStore` holds config + execution state + result data in one slice; `pivot-view.tsx:56` shallow-selects 3 fields but any config change re-triggers the whole `useShallow` set
- Split into 3 stores → pivot-config/pivot-view/pivot-grid each subscribe narrow
- Files: `src/stores/pivot.ts:12-23`, `src/components/pivot-view.tsx:56`, `src/components/pivot-config.tsx:82`

### Cluster E — dead/forced code (X-1 + X-2 + X-3 + X-4 + X-5 + X-7)
- `page.tsx:76` `sqlText` set-only state (dead)
- `pivot-view.tsx:68` empty `handlePivotExecute` + caller at `pivot-config.tsx:120`
- `pivot-view.tsx:131` `key={resolved}` force-remount (should be internal effect)
- `grid-view.tsx:77-83` `handleSqlGenerated` only setSqlText (linked to dead state)
- `agent-chat.tsx:20` `welcomeContent` eagerly evaluated
- `page.tsx:132-162` 3 hardcoded Tab buttons

### Standalone candidates not clustered (defer to future loops)
- M-2 `calculated-indicator-dialog.tsx` (599 lines) — large but cohesive form/dialog
- M-3 `create-widget-sql-dialog.tsx` (387 lines) — dashboard widget form
- M-4 `data-grid.tsx` (286 lines) — colWidth heuristics + dual empty/full template
- M-5 `pivot-view.tsx` DrilldownDrawer cohabitation (S-3)
- M-6 `assistant-message.tsx` 4 render branches + IIFE (S-7)
- I-3 PivotConfigPanel 3-tuple → TableRef
- D-2 `useDashboardsStore` 15-field shallow + `.find()`
- D-4 `useAgentChatsStore` whole-map persist
- S-4 `data-grid.tsx` dual SearchBar template
- C-2 `dashboard-selector-dialog.tsx` widget-creation side effect
- C-3 `data-grid.tsx:57-76` colWidth heuristic → hook
- C-4 `assistant-message.tsx:121-141` follow-up IIFE → useFollowUp hook
- C-5 `sql-console.tsx:96-99` global `<style>` injection
- L-3 QueryPanels/HistoryPanel/SavedPanel/CommunityPanel same-shape lists
- L-4 DataGrid 3-callers leverage

## 2. Grill

Walked decision tree per cluster. Terms sharpened against actual file shapes (read `pivot-config.tsx` 476L, `pivot.ts` 233L, `pivot-view.tsx` 216L, `grid-view.tsx` 155L, `assistant-message.tsx` 144L, `lang-provider.tsx` 40L, `agent-chat.tsx` 58L, `page.tsx` 239L).

### Cluster A — `pivot-config.tsx` god-component
- **Sharpened term**: _Config-editor panel cohabiting with history browser, two dialog hosts, domain constants, and a hidden cross-store write_.
- **Concrete bugs surfaced**:
  - `usePivotStore()` whole-object destructure at `pivot-config.tsx:66-82` (14 fields) — every execution/result change re-renders the entire config panel. `pivot-view.tsx:56-60` already proved the narrow-selector pattern works for the sibling panel.
  - Cross-store write `useDatasetStore.getState().selectTable(entry.tableName)` at `pivot-config.tsx:384` inside `HistoryPanel.onSelect` — bypasses React flow, hidden side effect. Should route through an orchestrator or hook.
  - 4 near-identical `<Select>` blocks (filters l:177-191, rows l:201-214, columns l:224-237, indicators l:298-317) — same `SelectTrigger` className, same schema.filter().map() shape. Copy-paste drift risk.
  - Domain constants `AGGREGATION_OPTIONS`/`aggregationShortLabel`/`formatLabel` (l:31-50) live in the component file — should be in `src/lib/`.
- **Existing seams to reuse**: `usePivotOrchestrator(schema, table, db)` hook already extracted (l:88). `useLang()` already used (l:59). `PivotFilterChip`/`HistoryPanel` already separate components. Decomposition is in progress, just incomplete.
- **Plain-English solution**: Extract (1) toolbar, (2) 5 Section blocks as sub-components in same file or co-located, (3) 2 dialogs into `pivot-config-dialogs.tsx`, (4) constants into `src/lib/pivot-aggregation-options.ts`, (5) replace whole-object `usePivotStore()` with narrow selectors, (6) route `selectTable` side-effect through `usePivotOrchestrator` or a `useHistorySelect()` hook.
- **Rule conflicts**: none. Reuses CONTEXT.md Pivot Execution Lifecycle seam. Minimal-diff rule honored (extractions are moves, not rewrites).
- **Test surface**: `npx tsc --noEmit` + `npm run lint` + manual dev server (pivot flow: add dim/indicator → execute → view SQL → history select). 6 extractions = 6 red→green cycles.
- **Risk**: medium. DnD context boundaries must stay intact (DroppableZone ids). Dialog state hoisting (showCalcDialog/editingCalc/formatIndicatorKey) needs care.

### Cluster B — prop-drilling interfaces
- **Sharpened term**: _Views receive raw lifecycle/internal state bags instead of subscribing to the lifecycle seam_.
- **Concrete bugs surfaced**:
  - `GridView` 16 props (`grid-view.tsx:13-32`) — receives `sort`/`searchQuery`/`pendingAutoExecute`/`loadedRows` + 7 callbacks. The `sql`+`pendingAutoExecute`+`onSetPendingAutoExecute` trio is a deferred-execute mechanism leaking into the view.
  - `page.tsx:55-74` destructures 16 flat fields from `useQueryOrchestrator()` then re-fans to `GridView` — page is a pure pass-through.
  - `AssistantMessage` 14 props — receives full `messages` array (needed only to find prev/next user msgs at l:122-124) + `aiFollowUpQuestions` + 4 callbacks. The follow-up IIFE at l:121-141 is logic, not rendering — pure hook candidate.
- **Existing seams to reuse**: CONTEXT.md QueryLifecycle (`execute/sort/setSearchQuery/loadMore/getFilteredRows/subscribe`, seam `QueryLifecycleDeps`). `useQueryOrchestrator` already wraps it — just returns a flat bag.
- **Plain-English solution**: (1) `GridView` subscribes via `useQueryOrchestrator()` (or a narrower `useQueryLifecycleView()` hook), (2) `AssistantMessage` receives `useMessageFollowUp(msg)` hook + subscribes to agent-chat store directly, (3) `page.tsx` stops destructuring 16 fields.
- **Rule conflicts**: none direct. CONTEXT.md QueryLifecycle seam explicitly supports this.
- **Test surface**: type-check + manual full-grid-flow + agent-chat-flow. Large surface.
- **Risk**: **high**. Changes public interface of 3 high-traffic components + page orchestration + orchestrator return shape. Cascading prop-type changes. Most likely to leave broken state mid-refactor.

### Cluster C — `_t`/`lang` prop drilling
- **Sharpened term**: _Redundant prop drilling past an existing context_.
- **Concrete facts**: `lang-provider.tsx:19-21` exports `useLang()` returning `{lang, setLang, _t}`. `AgentChat` already calls it (l:19) then passes `_t`+`lang` to 4 children. `AssistantMessage` receives and re-passes to 6 children. 11 files, 22 prop slots.
- **Nuance**: `lang` is also a domain value (not just `_t` carrier) — `stripMarkdownTables(msg.content, lang)` (assistant-message l:55), `suggestFollowUp(..., lang)` (l:126). Components still need `lang` value, but can self-subscribe via `useLang()`.
- **Re-render analysis**: context re-renders all consumers on lang change. But current prop-drilling ALSO re-renders all on lang change (parent re-renders → children re-render). Net zero. Context may be better — only `_t`/`lang` consumers re-render, not all AgentChat children.
- **Plain-English solution**: Delete `_t`+`lang` from props of 11 components; each calls `useLang()`.
- **Rule conflicts**: none. `useLang()` is canonical.
- **Test surface**: type-check only. Smallest surface of all clusters.
- **Risk**: very low. Pure deletion. But: **low architectural depth gain** — hygiene, not deepening.

### Cluster D — `pivot.ts` three duties
- **Sharpened term**: _Single store holding config + execution state + result data — three unrelated change frequencies_.
- **Concrete facts**: `usePivotStore` (`pivot.ts:12-48`) holds config (rows/cols/indicators/calc/filters/sort/totals) + execution (isExecuting/error/lastSQL) + result (resultData). `partialize` (l:221-229) already persists ONLY config — the natural seam is already drawn. `pivot-view.tsx:56-60` uses `useShallow` for 3 execution/result fields. `pivot-config.tsx:66-82` whole-object destructures 14 fields.
- **Existing seams**: CONTEXT.md Pivot Execution Lifecycle (`runPivotExecution(input, deps)→AsyncGenerator<PivotExecutionEvent>`, events started/succeeded/error/aborted, seam `PivotExecutionDeps`). `migrate-pivot-store.ts` exists (l:5) — extend for split.
- **Plain-English solution**: Split into `usePivotConfigStore` (persist) + `usePivotExecutionStore` (transient) + `usePivotResultStore` (transient). Or 2: config vs execution+result. Update 3 consumers. Extend `migrate-pivot-store.ts` for cross-store reset in `loadConfig`.
- **Rule conflicts**: none. Aligns with CONTEXT.md seam.
- **Test surface**: type-check + manual pivot flow (config → execute → result → history load). Persist migration test (manual: load old localStorage). Medium.
- **Risk**: medium. Persist migration + `loadConfig` cross-store reset + 3 consumer updates. Single-store currently isn't actively broken (shallow selectors mitigate).

### Cluster E — dead/forced code
- **Sharpened term**: _Set-only state, empty handlers, and key-swap remounts masking missing effects_.
- **Concrete bugs surfaced**:
  - `page.tsx:76` `const [sqlText, setSqlText] = useState("")` — `setSqlText` called at l:80 in `handleAgentSqlGenerated`, but `sqlText` NEVER READ in page.tsx. Dead state. Intent was agent→page→GridView SQL sync, but `pendingAutoExecute` (l:23) is the actual mechanism. `GridView` has its OWN `sqlText` (l:55) that IS used.
  - `onSqlGenerated` callback chain: page l:185→GridView l:30,51,77-83→`handleSqlGenerated` sets local sqlText (useful, shows in console) + calls dead parent `onSqlGenerated`. Parent half is dead.
  - `pivot-view.tsx:68` `const handlePivotExecute = useCallback(() => {}, [])` — empty no-op passed as `onExecute` to `PivotConfigPanel` (l:118). `PivotConfigPanel.handleExecute` (l:118-121) calls `executePivot()` then `onExecute()` — the `onExecute()` call is a no-op. Dead.
  - `pivot-view.tsx:131` `key={resolved}` — force-remount PivotGrid on theme change. Loses internal state (scroll, selection). Should be internal `useEffect` in PivotGrid.
  - `agent-chat.tsx:20` `welcomeContent = _t("agent.welcome")` — eager eval every render. Minor.
- **Plain-English solution**: (1) Delete `sqlText`/`setSqlText`/`handleSqlGenerated` in page.tsx; keep GridView's local. (2) Delete `handlePivotExecute` empty fn + `onExecute` prop from PivotConfigPanel (call `onExecute` only if needed — it's not). (3) Move `key={resolved}` into PivotGrid as `useEffect([resolved])`. (4) Lazy-init `welcomeContent` inside `useAgentChat` if non-null.
- **Rule conflicts**: none.
- **Test surface**: type-check + manual (grid SQL execute, pivot execute, theme toggle). Small.
- **Risk**: low. Pure deletion + one effect migration. But: **low architectural depth gain** — hygiene.

## 3. Selection

Scored 5 clusters against rule set. Dimensions: **depth gain** (architectural leverage, primary per skill), **rule fit** (AGENTS.md/CONTEXT.md alignment), **test surface** (smaller = safer without test framework), **risk** (lower = better), **bug fix count** (concrete defects addressed).

| Cluster | Depth | Rule fit | Test surface | Risk | Bugs | Verdict |
|---|---|---|---|---|---|---|
| A pivot-config god | high | ✅ reuses orchestrator seam | medium (6 extractions) | medium | 3 (perf/correctness/DRY) | **WINNER** |
| B prop-drilling | highest | ✅ QueryLifecycle seam | large (3 hi-traffic comps) | high | 2 | runner-up, defer |
| C _t/lang drilling | low | ✅ canonical context | tiny | very low | 0 | hygiene, separate loop |
| D pivot store split | medium | ✅ Pivot Exec Lifecycle seam | medium (persist migration) | medium | 0 (theoretical) | defer |
| E dead/forced code | low | ✅ | small | low | 4 | hygiene, fold into A or separate |

**Winner: Cluster A — `pivot-config.tsx` god-component decomposition.**

Rationale:
1. **Highest depth-to-risk ratio** among deepening clusters. Self-contained (one file → 4-5 files), no cross-component interface changes.
2. **Concrete bugs** (not theoretical): whole-object subscription perf bug, cross-store write correctness bug, 4× Select DRY violation.
3. **Existing seam to extend**: `usePivotOrchestrator` already extracted — decomposition is in progress, we finish it.
4. **6 clean red→green cycles** fit the no-test-framework constraint (each extraction verifiable via tsc + manual).
5. **Reusable in future loops**: extracted `pivot-aggregation-options.ts` + dialog components + Section primitives usable by M-2/M-3/M-5 (deferred standalone candidates).

**Folded in from Cluster E**: the `handlePivotExecute` empty no-op (pivot-view.tsx:68) + its `onExecute` prop drilling — directly touches Cluster A's `PivotConfigPanel` interface. Cheap to fix while decomposing.

Deferred to future loops: B (high risk, separate loop), C (hygiene), D (theoretical), E-remainder (page.sqlText + key={resolved} + welcomeContent — separate small loop).

## 4. Plan

Phased plan for Cluster A decomposition + folded E fix. Each phase = one red→green cycle verified via `npx tsc --noEmit` + `npm run lint` + manual dev server (port 4000) pivot flow. No test framework (per AGENTS.md).

**Target file**: `src/components/pivot-config.tsx` (476 lines → ~250 lines after)
**New files**: `src/lib/pivot-aggregation-options.ts`, `src/components/pivot-config-dialogs.tsx`, `src/components/pivot-config-sections.tsx`, `src/components/pivot-field-select.tsx`

### Phase P1 — Extract domain constants
- **Move** `AGGREGATION_OPTIONS` (l:31-38), `aggregationShortLabel` (l:40-42), `formatLabel` (l:44-50) → `src/lib/pivot-aggregation-options.ts`
- **Export** them; update `pivot-config.tsx` import
- **Verify**: tsc + lint + manual (indicator aggregation dropdown still shows SUM/AVG/COUNT/...)
- **Risk**: trivial. Pure move.

### Phase P2 — Extract `PivotConfigToolbar`
- **Move** top toolbar JSX (l:126-155: Execute/Stop/ViewSQL buttons + isExecuting branch) → new component in `pivot-config.tsx` (co-located, below main export) or separate file
- **Props**: `isExecuting: boolean`, `onExecute: () => void`, `onCancel: () => void`, `onViewSql: () => void`
- **Verify**: tsc + lint + manual (Execute button triggers pivot, Stop cancels, View SQL opens preview)
- **Risk**: low. Self-contained JSX block.

### Phase P3 — Extract dialog hosts → `src/components/pivot-config-dialogs.tsx`
- **Move** `CalculatedIndicatorDialog` (l:392-409) + `IndicatorFormatDialog` (l:411-421) wrappers
- **Hoist** dialog state (`showCalcDialog`, `editingCalc`, `formatIndicatorKey`) into a `usePivotConfigDialogs()` hook (co-located in dialogs file)
- **Props** to main panel: hook return `{ showCalcDialog, editingCalc, formatIndicatorKey, setShowCalcDialog, setEditingCalc, setFormatIndicatorKey, formatIndicator, calcOnSave, formatOnSave }`
- **Verify**: tsc + lint + manual (add calc indicator → dialog opens → save → list updates; click format → dialog → save → format applies)
- **Risk**: medium. State hoisting — ensure onSave callbacks still reference live `indicators`/`calculatedIndicators`/`updateIndicator`/`addCalculatedIndicator`/`updateCalculatedIndicator`.

### Phase P4 — Extract `PivotFieldSelect` + 5 Section sub-components
- **Extract** `PivotFieldSelect` (reusable Select: props `value`, `onValueChange`, `placeholder`, `options: {name,type}[]`) — collapses 4 copy-pasted Select blocks (filters l:177-191, rows l:201-214, columns l:224-237, indicators l:298-317)
- **Extract** `FiltersSection`, `RowsSection`, `ColumnsSection`, `IndicatorsSection`, `CalculatedSection` → `src/components/pivot-config-sections.tsx`
- Each Section receives narrow props (its own slice of config + handlers)
- **Verify**: tsc + lint + manual (add/remove filter, add/remove row dim, add/remove col dim, add/remove indicator, add/remove calc indicator)
- **Risk**: medium. DnD `DroppableZone` ids must stay `zone:filters`/`zone:rows`/`zone:columns`. Section boundaries must not break DnD context (DndContext lives in pivot-view.tsx parent — sections are children of it).

### Phase P5 — Replace whole-object `usePivotStore()` with narrow selectors
- **Current**: `pivot-config.tsx:66-82` destructures 14 fields from `usePivotStore()` — every execution/result change re-renders entire config panel
- **New**: split into targeted selectors. Config fields via `useShallow`:
  ```ts
  const { rows, columns, indicators, calculatedIndicators, filters } = usePivotStore(useShallow(s => ({ rows: s.rows, columns: s.columns, indicators: s.indicators, calculatedIndicators: s.calculatedIndicators, filters: s.filters })))
  const removeRow = usePivotStore(s => s.removeRow)  // stable ref
  // ... individual action selectors (Zustand returns stable refs for actions)
  const loadConfig = usePivotStore(s => s.loadConfig)
  ```
- **Verify**: tsc + lint + manual (execute pivot → config panel does NOT re-render mid-execution; add indicator → only IndicatorsSection re-renders)
- **Risk**: low-mid. Must ensure no stale closures. Actions are stable in Zustand. `useShallow` for config group.

### Phase P6 — Route `selectTable` side-effect through hook
- **Current**: `pivot-config.tsx:384` `useDatasetStore.getState().selectTable(entry.tableName)` — direct cross-store write inside `HistoryPanel.onSelect`
- **New**: add `selectTableFromHistory(entry)` to `usePivotOrchestrator` (or new `useHistorySelect()` hook) — encapsulates `loadConfig(entry.config)` + `selectTable(entry.tableName)` + any future side effects
- **Verify**: tsc + lint + manual (click history item → config loads + table switches in sidebar)
- **Risk**: low. Moves 2 calls into a hook. Behavior identical.

### Phase P7 (folded E) — Remove dead `handlePivotExecute` + `onExecute` prop
- **Delete** `pivot-view.tsx:68` `const handlePivotExecute = useCallback(() => {}, [])`
- **Delete** `onExecute` from `PivotConfigPanelProps` (l:27) + from `handleExecute` call (l:120)
- **Simplify** `PivotConfigPanel.handleExecute` (l:118-121) → just `executePivot()` (no no-op callback)
- **Delete** `onExecute={handlePivotExecute}` prop pass at `pivot-view.tsx:118`
- **Verify**: tsc + lint + manual (Execute button still runs pivot)
- **Risk**: trivial. Pure deletion of dead code.

### Verification gates (after each phase)
1. `npx tsc --noEmit` — type-check passes
2. `npm run lint` — ESLint 9 flat config passes
3. Manual dev server (`npm run dev`, port 4000): pivot flow — select table → add row/col dim → add indicator → Execute → View SQL → history select → calc indicator dialog → format dialog

### Out of scope (explicit non-goals)
- No store split (Cluster D — deferred)
- No `_t`/`lang` prop deletion (Cluster C — deferred)
- No `page.tsx` sqlText dead state removal (E-remainder — deferred)
- No `key={resolved}` migration (E-remainder — deferred)
- No `GridView`/`AssistantMessage` prop interface changes (Cluster B — deferred)
- No new tests (no test framework)

### @oracle review — `ses_0c5b69f64ffee35e4iBV6dPCVe` (alias `ora-1`)
All 7 phases GO. Phase ordering confirmed P1→P2→P3→P4→P5→P6→P7. Refinements adopted:

- **P1 rename**: `src/lib/pivot-aggregation-options.ts` → `src/lib/pivot-config-constants.ts` (because `formatLabel` not aggregation-specific). Add TODO comment for pre-existing i18n gap (hardcoded Chinese strings in `formatLabel`).
- **P3 split into P3a + P3b**: 
  - P3a: create `usePivotConfigDialogs()` hook in-place within `PivotConfigPanel` (verify `formatIndicator` derivation, `calcOnSave`/`formatOnSave` closures, `key` remount pattern). Hook subscribes to `usePivotStore(s => s.indicators)` for `formatIndicator`.
  - P3b: move dialog JSX wrappers to `src/components/pivot-config-dialogs.tsx`.
  - `indicatorSelectValue` (l:63) stays in `PivotConfigPanel` — NOT dialog state.
  - Hook called once in `PivotConfigPanel`, values passed to both CalculatedSection + dialog wrapper.
- **P4 split into P4a + P4b**:
  - P4a: `PivotFieldSelect` with `options: {name, type}[]` prop (each Section pre-computes its own filtered options) + optional `value`/`onValueChange` for controlled indicator case (l:298-304 forced reset).
  - P4b: extract Sections ONE AT A TIME: FiltersSection → RowsSection → ColumnsSection → IndicatorsSection → CalculatedSection.
  - **Move `DroppableZone` to `pivot-config-sections.tsx`** (only used there; avoid confusing dependency direction). DroppableZone ids (`zone:filters`/`zone:rows`/`zone:columns`) unchanged. No DnD provider boundary risk (DndContext in `pivot-view.tsx:106` wraps entire `PivotConfigPanel`).
- **P5**: standard Zustand pattern. `commitIndicatorTitle` (l:104-116) still recreates on draft edit (pre-existing, out of scope).
- **P6 split**: recommend separate `useHistorySelect()` hook (not orchestrator widening — cohesion). Encapsulates `loadConfig(entry.config)` + `selectTable(entry.tableName)`. Keeps `usePivotOrchestrator` focused.
- **P7**: also clean up `PivotConfigPanelProps` interface — remove `onExecute` field.

### TDD adaptation (no test framework per AGENTS.md)
Red→green = `npx tsc --noEmit` fail→pass + `npm run lint` fail→pass + manual dev server (port 4000) pivot flow verify. No unit tests. Each phase commits to "green" before next phase starts.

## 5. Implementation

**Fixer task**: `ses_0c5b1d04fffelGaNqwEsNwHhf7` (alias `fix-1`, completed).
All 9 phases executed strictly sequentially, each verified green before next. Zero deviations from plan.

### Files touched

| File | Status | Δ Lines | Notes |
|---|---|---|---|
| `src/lib/pivot-config-constants.ts` | NEW | +23 | `AGGREGATION_OPTIONS` + `aggregationShortLabel` + `formatLabel` + TODO(i18n) |
| `src/components/pivot-config-toolbar.tsx` | NEW | +53 | `PivotConfigToolbar` (Execute/Cancel/View SQL) |
| `src/components/pivot-config-dialogs.tsx` | NEW | +63 | `PivotConfigDialogs` (Calculated + Format wrappers) |
| `src/components/pivot-field-select.tsx` | NEW | +39 | `PivotFieldSelect` shared — replaces 4 inline `<Select>` blocks |
| `src/components/pivot-config-sections.tsx` | NEW | +349 | 5 Sections + `DroppableZone`/`Section`/`Tag` helpers |
| `src/components/pivot-config.tsx` | MODIFIED | 476→259 (-217) | Orchestration-only: `usePivotConfigDialogs()` + `useHistorySelect()` in-place + narrow selectors (`useShallow` + individual action selectors) |
| `src/components/pivot-view.tsx` | MODIFIED | 216→213 (-3) | Dead `handlePivotExecute` + `onExecute` prop removed |

### Phase-by-phase verification

| Phase | File | Δ | tsc | lint | Notes |
|---|---|---|---|---|---|
| P1 | constants + pivot-config.tsx | +23/-19 | ✅ | ✅ | TODO(i18n) comment added for hardcoded "百分比"/"货币"/"数字" |
| P2 | toolbar + pivot-config.tsx | +53/-30 | ✅ | ✅ | Pure JSX extraction |
| P3a | in-place `usePivotConfigDialogs()` hook | +44 | ✅ | ✅ | Subscribes `usePivotStore(s => s.indicators)` for `formatIndicator`; `indicatorSelectValue` stays in panel |
| P3b | pivot-config-dialogs.tsx + pivot-config.tsx | +63/-27 | ✅ | ✅ | `key` prop remount patterns preserved exactly |
| P4a | pivot-field-select.tsx + 4 replacements | +39/-56 | ✅ | ✅ | Each Section pre-computes own filtered options; controlled indicator case via `value`+`onValueChange` |
| P4b | pivot-config-sections.tsx + pivot-config.tsx | +349/-175 | ✅ | ✅ | 5 Sections extracted one-at-a-time; `DroppableZone` moved; ids `zone:filters`/`zone:rows`/`zone:columns` unchanged |
| P5 | narrow selectors (useShallow + individual) | ~0 | ✅ | ✅ | `commitIndicatorTitle` still recreates on draft edit (pre-existing, out of scope) |
| P6 | in-place `useHistorySelect()` hook | +13/-9 | ✅ | ✅ | Encapsulates `loadConfig(entry.config)` + `selectTable(entry.tableName)`; orchestrator not widened |
| P7 | remove dead `onExecute` | -3 (pivot-view) | ✅ | ✅ | `PivotConfigPanelProps.onExecute` removed; `executePivot()` handles all side effects |

**Net delta**: pivot-config.tsx 476L → 259L (-217L, -45.6%). 5 new focused modules. pivot-view.tsx -3L (dead code). Total new code: 527L across 5 files (avg ~105L per module — single-responsibility).

## 6. Verify + close

### Automated gates (per AGENTS.md rule)
- `npx tsc --noEmit`: ✅ 0 errors (final state after all 9 phases)
- `npm run lint`: ✅ 0 errors, 32 warnings (pre-existing, unchanged from baseline)
- No test framework configured — no tests to run (per AGENTS.md)

### Manual smoke test
- **Status**: SKIPPED — no ClickHouse running locally, cannot exercise live pivot flow (db/table/schema fetch + execute).
- **Required manual verify (next session with ClickHouse up)**: `npm run dev` → http://localhost:4000 → 透视 tab → select table → add row/col dim → add indicator → Execute → View SQL → history select → calc indicator dialog → format dialog.
- **Risk of unverified**: low. Each phase verified independently green via tsc+lint. Extractions are pure moves (P1/P2/P3b/P4a/P4b) or behavior-preserving substitutions (P5 narrow selectors, P6 hook, P7 dead-code removal). No algorithmic changes, no API surface changes, no store mutations.

### Rule compliance
- ✅ `AGENTS.md`: `"use client"` preserved in all new files; `@/*` alias used; shadcn/ui base-nova preserved; `@tanstack/react-virtual` untouched; no `@tanstack/react-table` imports added.
- ✅ `core.md`: 中文回复 maintained; strict TS (no `as any`/`@ts-ignore`); minimal diff (moves, not rewrites); never committed.
- ✅ `CONTEXT.md`: Pivot Execution Lifecycle seam (`usePivotOrchestrator`) preserved — not widened. New modules align with existing decomposition pattern (PivotFilterChip/HistoryPanel already separate).
- ✅ TDD adaptation: 9 red→green cycles via tsc+lint gates.

### Bugs fixed (from grill Cluster A list)
1. **Perf**: whole-object `usePivotStore()` destructure (14 fields) re-rendered entire config panel on every execution/result change → P5 narrow selectors (useShallow + individual action selectors).
2. **Correctness**: cross-store write `useDatasetStore.getState().selectTable()` hidden in `HistoryPanel.onSelect` → P6 `useHistorySelect()` hook encapsulates.
3. **DRY**: 4 copy-pasted `<Select>` blocks → P4a `PivotFieldSelect` shared component.
4. **Dead code**: empty `handlePivotExecute` + `onExecute` prop drilling → P7 removed.

### Deferred (future loops)
- Cluster B (prop-drilling interfaces — `GridView`/`AssistantMessage`/`page.tsx`)
- Cluster C (`_t`/`lang` prop drilling — 11 agent-chat subtree files)
- Cluster D (`pivot.ts` store split — config/execution/result)
- Cluster E-remainder (`page.tsx` `sqlText` dead state, `key={resolved}` force-remount, `agent-chat.tsx` eager `welcomeContent`)
- Standalone: M-2/M-3/M-4/M-5/M-6, I-3, D-2, D-4, S-4, C-2/C-3/C-4/C-5, L-3/L-4

### Reusable sessions (for follow-up loops)
- `exp-1` / `ses_0c5bd0122ffezhQhtfgcnmFEmO` (explorer) — frontend panels map
- `ora-1` / `ses_0c5b69f64ffee35e4iBV6dPCVe` (oracle) — pivot decomposition plan reviewer
- `fix-1` / `ses_0c5b1d04fffelGaNqwEsNwHhf7` (fixer) — pivot-config decomposition implementer

## Loop closed.
