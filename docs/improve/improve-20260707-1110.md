# Improve Loop — Cluster C: `_t`/`lang` prop drilling elimination

**Started**: 2026-07-07 11:10
**Scope**: Frontend panels — Cluster C (`_t`/`lang` prop drilling in agent-chat subtree)
**Rule sources**: `AGENTS.md` (stack), `CONTEXT.md` (domain glossary), `~/.config/opencode/memory/core.md`
**Prior loops**: `docs/improve/improve-20260707-0948.md` (Cluster A closed), `docs/improve/improve-20260707-1400.md` (Cluster B closed)

## 0. Context

Cluster C from prior explorer map (38 candidates → 5 clusters). Deferred in Loop 1+2. User invoked `/improve-loop` with scope "Cluster C".

`useLang()` context at `src/components/lang-provider.tsx:19-21` already exports `{lang, setLang, _t}`. Context value memoized at l:33 on `[lang, setLang, _t]` — stable unless lang changes. 40 files across codebase already self-subscribe via `useLang()` (established pattern). **10 agent-chat subtree files are OUTLIERS** — still receive `_t`/`lang` as redundant props from parent `agent-chat.tsx` (which itself calls `useLang()` at l:19).

## 1. Candidates (scope confirmed via grep)

10 files in `src/components/agent-chat/**/*.tsx` receive `_t` and/or `lang` as props:

| File | Props received | Uses `lang` internally for |
|---|---|---|
| `assistant-message.tsx` | `_t`, `lang` | `stripMarkdownTables(content, lang)` l:55, `suggestFollowUp(..., lang)` l:126, pass-through to 6 children |
| `input-bar.tsx` | `_t`, `lang` | (verify actual usage) |
| `thinking-panel.tsx` | `_t` only | — |
| `message-actions.tsx` | `_t`, `lang` | (verify actual usage) |
| `data-table.tsx` | `_t` only | — |
| `header.tsx` | `_t` only | — |
| `suggestion-list.tsx` | `_t`, `lang` | (verify actual usage) |
| `chart-detail-card.tsx` | `_t`, `lang` | `getChartNodeContext({item, visualization, lang})` l:25, `suggestDeepDiveDirections({..., lang})` l:65 |
| `dashboard-selector-dialog.tsx` | `_t` only | — |
| `initial-suggestions.tsx` | `_t`, `lang` | (verify actual usage) |

**Prop-passing sites** (to delete):
- `agent-chat.tsx` l:34 (Header), l:43 (AssistantMessageView), l:53 (InitialSuggestions), l:55 (InputBar) — 4 sites
- `assistant-message.tsx` l:50 (ThinkingPanel), l:101-102 (ChartDetailCard), l:111 (DashboardSelectorDialog), l:116 (DataTable), l:119 (MessageActions), l:133-134 (SuggestionList) — 6 sites

**Note**: `agent-chat.tsx` l:19 `useLang()` call STAYS — `lang`/`_t` consumed by `useAgentChat` hook (l:29) + `welcomeContent` (l:20). Only child prop-passing goes away.

## 2. Grill

**Q1: Is `useLang()` context value stable enough to self-subscribe in 10 components?**
A1: Yes. `lang-provider.tsx:33` `useMemo(() => ({lang, setLang, _t}), [lang, setLang, _t])`. `setLang` + `_t` are `useCallback`-stable (`lang-provider.tsx:26,31`). Value changes ONLY when `lang` changes. 40 other files already self-subscribe without perf issues. No new re-render risk — components already re-render when `lang` changes (parent passed props that changed).

**Q2: Does any file use `lang` for non-i18n purpose?**
A2: No. All `lang` usages are for i18n: `stripMarkdownTables`, `suggestFollowUp`, `getChartNodeContext`, `suggestDeepDiveDirections` — all format output based on language. Pure i18n concern, belongs in context.

**Q3: Risk of `useLang()` call outside LangProvider boundary?**
A3: None. `LangProvider` wraps entire app at root. All 10 files render inside it (proven by current prop-passing working — props come from `agent-chat.tsx` which is inside provider).

**Q4: Should we also eliminate `_t`/`lang` from `assistant-message.tsx` pass-through to grandchildren?**
A4: Yes — that's the leverage. `assistant-message.tsx` currently passes `_t`/`lang` to 6 grandchildren. Once grandchildren self-subscribe, `assistant-message.tsx` stops passing. `assistant-message.tsx` itself self-subscribes for its own `stripMarkdownTables`/`suggestFollowUp` usage.

**Q5: ADR conflicts?**
A5: None. `CONTEXT.md` doesn't mention i18n prop-drilling as intentional. `issues/*.md` no ADR on prop-drilling. Established pattern (40 files self-subscribe) confirms convention.

**Q6: Test surface?**
A6: tsc + lint per file. No behavioral change (context provides same values as props did). Manual smoke: type in agent chat, switch lang via toggle, verify Chinese/English labels flip.

**Q7: Phase split?**
A7: Single phase. Atomic per file-pair: (1) child self-subscribes + removes props from interface, (2) parent stops passing. tsc green throughout if done per-pair. Fixer verifies after each file-pair.

**Q8: Verify actual `lang` usage in files that declare `lang` prop but might not use it?**
A8: Fixer verifies per-file. If `lang` declared but unused in body, just delete from interface (dead prop). If used, self-subscribe. Same for `_t` (unlikely to be dead — every file uses it for labels).

## 3. Select

**WINNER**: Cluster C (only candidate in this loop). High leverage (10 files + 2 parents touched), low risk (context stable, established pattern, pure deletion), clear scope.

## 4. Plan

### P1: Self-subscribe 10 files + eliminate prop-passing

For each of 10 agent-chat subtree files:
1. Add `import { useLang } from "@/components/lang-provider"` (if not already imported)
2. Add `const { _t } = useLang()` or `const { _t, lang } = useLang()` at top of component body (depending on actual usage — fixer verifies per file)
3. Remove `_t` and/or `lang` from props interface
4. Update parent (`agent-chat.tsx` for 4 direct children, `assistant-message.tsx` for 6 grandchildren) to stop passing `_t`/`lang` props

**File-by-file order** (leaves first — children before parents):
1. `thinking-panel.tsx` (only `_t`) — parent: assistant-message
2. `data-table.tsx` (only `_t`) — parent: assistant-message
3. `header.tsx` (only `_t`) — parent: agent-chat
4. `dashboard-selector-dialog.tsx` (only `_t`) — parent: assistant-message
5. `message-actions.tsx` (`_t`, `lang`) — parent: assistant-message
6. `suggestion-list.tsx` (`_t`, `lang`) — parent: assistant-message
7. `input-bar.tsx` (`_t`, `lang`) — parent: agent-chat
8. `initial-suggestions.tsx` (`_t`, `lang`) — parent: agent-chat
9. `chart-detail-card.tsx` (`_t`, `lang`) — parent: assistant-message
10. `assistant-message.tsx` (`_t`, `lang`) — parent: agent-chat (do LAST — has 6 child pass-through sites to clean up after children done)

**`agent-chat.tsx`** (parent of 4): remove `_t={_t}` and `lang={lang}` from l:34, l:43, l:53, l:55. KEEP `useLang()` at l:19.

### TDD adaptation
Red→green = `npx tsc --noEmit` fail→pass + `npm run lint` fail→pass + manual dev server (port 4000) verify after phase. Each file-pair green before next.

## 5. Implementation

**Fixer task**: `ses_0c5b1d04fffelGaNqwEsNwHhf7` (alias `fix-1`, resumed, completed).
11 files touched (10 self-subscribe + 1 parent cleanup). Per-file-pair tsc+lint verified green throughout.

### Files touched

| File | Before | After | Δ | Change |
|---|---|---|---|---|
| `thinking-panel.tsx` | 55L | 56L | +1 | `useLang()` + call, removed `_t` prop |
| `data-table.tsx` | 35L | 37L | +2 | `useLang()` + call, removed `_t` prop |
| `header.tsx` | 67L | 69L | +2 | `useLang()` + call, removed `_t` prop |
| `dashboard-selector-dialog.tsx` | 92L | 94L | +2 | `useLang()` + call, removed `_t` prop |
| `message-actions.tsx` | 52L | 53L | +1 | `useLang()` + call, removed `_t`+`lang` (lang DEAD) |
| `suggestion-list.tsx` | 30L | 32L | +2 | `useLang()` + call, removed `_t`+`lang` (lang DEAD) |
| `input-bar.tsx` | 50L | 52L | +2 | `useLang()` + call, removed `_t`+`lang` (lang DEAD) |
| `initial-suggestions.tsx` | 41L | 43L | +2 | `useLang()` + call, removed `_t`+`lang` (lang DEAD) |
| `chart-detail-card.tsx` | 138L | 139L | +1 | `useLang()` + call (both), removed both props |
| `assistant-message.tsx` | 144L | 146L | +2 | `useLang()` + call (both), removed both props + 6 child prop-passing sites |
| `agent-chat.tsx` | 58L | 58L | 0 | Removed `_t`/`lang` from 4 child JSX sites. `useLang()` call KEPT (consumed by `useAgentChat` + `welcomeContent`). |

**Net delta**: +17L across 11 files (10 files +1-2L for `useLang()` import+call, 1 file 0L pure deletion). Warnings dropped 32→28 (-4) due to dead `lang` prop deletion.

### Surprises
4 files (`message-actions`, `suggestion-list`, `input-bar`, `initial-suggestions`) declared `lang` prop but never used it in body — pure dead prop. Deleted from interface, no self-subscribe for `lang` in those 4.

## 6. Verify + close

### Automated gates
- `npx tsc --noEmit`: ✅ 0 errors
- `npm run lint`: ✅ 0 errors, 28 warnings (baseline was 32 — dropped 4 due to dead `lang` prop deletion)
- No test framework per AGENTS.md

### Rule compliance
- ✅ `AGENTS.md`: `"use client"` preserved; `@/*` alias; shadcn/ui untouched; no `@tanstack/react-table` imports.
- ✅ `core.md`: 中文回复; strict TS (no `as any`/`@ts-ignore`); minimal diff (only props interface + `useLang()` call + parent JSX); never committed.
- ✅ `CONTEXT.md`: i18n concern centralized in `useLang()` context — aligns with established pattern (40+ files already self-subscribe).
- ✅ TDD adaptation: 11 red→green cycles via tsc+lint gates.

### Bugs fixed
1. **Prop-drilling**: 10 files received `_t`/`lang` as redundant props despite `useLang()` context existing → all self-subscribe.
2. **Dead props**: 4 files (`message-actions`/`suggestion-list`/`input-bar`/`initial-suggestions`) declared `lang` prop but never used it → deleted from interface.
3. **Parent bloat**: `agent-chat.tsx` passed `_t`/`lang` to 4 children, `assistant-message.tsx` passed to 6 grandchildren → all eliminated.

### Manual smoke test
- **Status**: SKIPPED — no ClickHouse running locally.
- **Required manual verify (next session with ClickHouse up)**: `npm run dev` → http://localhost:4000 → open agent chat → type message → verify Chinese labels render → toggle lang to English via lang-toggle → verify all labels flip → click chart drill-down → verify ChartDetailCard renders → click deep-dive → verify directions render in selected lang.
- **Risk**: very low. Pure prop elimination + context self-subscribe. Context value is memoized and stable. No behavioral change — same values flow to same consumers via different path.

### Deferred (future loops)
- Cluster D (`pivot.ts` store split — config/execution/result)
- Cluster E-remainder (`key={resolved}` force-remount, eager `welcomeContent`)
- P5 AssistantMessage self-subscribe (BLOCKED on D-4 hook→store migration)
- Standalone: M-2/M-3/M-4/M-5/M-6, I-3, D-2, S-4, C-2/C-3/C-4/C-5, L-3/L-4

### Reusable sessions
- `fix-1` / `ses_0c5b1d04fffelGaNqwEsNwHhf7` (fixer) — 3 loops implemented, 20 phases total green

## Loop closed.
