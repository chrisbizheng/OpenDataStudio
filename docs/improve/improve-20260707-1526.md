# Improve Loop 7 — data-grid.tsx cluster (M-4 + C-3 + S-4)

**Date**: 2026-07-07 15:26
**Scope**: `src/components/data-grid.tsx` (286L) — colWidth heuristic + duplicate SearchBar + dead prop
**Rule sources**: AGENTS.md (stack), CONTEXT.md (domain glossary), `~/.config/opencode/memory/core.md`
**Test baseline**: 250 pass / 0 fail, tsc 0, lint 0 err/31 warn

## 0. Scope confirmation

User picked "data-grid.tsx cluster (M-4+C-3+S-4)" from 14 standalone candidates. 3 candidates folded into one file-scoped loop + 1 NEW dead prop discovered during read.

### Candidates in scope
- **C-3**: `colWidths` useMemo (L57-76) + `textWidth` helper (L280-285) → extract pure function. No component state deps.
- **S-4**: 2× duplicate SearchBar block (L107-112 empty template + L130-134 full template) → extract subcomponent.
- **M-4**: empty (L103-126) vs full (L128-277) template split. Early return already clean — LOW value, likely skip.
- **NEW (dead prop)**: `selectedTable` in `DataGridProps` interface L15 but never destructured at L30-44, never used in body. Delete from interface + 2 consumer call sites.

### Deferred (not in scope)
- L-4 "DataGrid 3-callers leverage" — STALE, only 2 consumers (`grid-view.tsx:90`, `pivot-view.tsx:199`). Different prop shapes (full vs minimal 4-prop). No leverage.

## 1. Architectural map

### `data-grid.tsx` (286L)
- L11-26 `DataGridProps` interface (14 fields) — `selectedTable` dead
- L28 `ROW_HEIGHT = 32` const
- L30-44 destructure (13 fields, `selectedTable` NOT destructured)
- L45 `useLang()` → `_t`
- L46 `tableContainerRef`
- L47 `hoveredCol` state
- L49-55 `gridColumns` useMemo (columns+schema → `{name,type,comment}[]`)
- L57-76 `colWidths` useMemo — C-3 target. Pure: `columns`+`rows`+`gridColumns` → `number[]` (first entry 28 for row-num col). Uses `textWidth` (L280) + `formatType` (imported).
- L78-85 `colStyle` useCallback (idx → CSSProperties)
- L87-94 `handleScroll` useCallback (load-more on scroll near bottom)
- L96-101 `rowVirtualizer` (`@tanstack/react-virtual`)
- L103-126 EMPTY template (rows.length===0): SearchBar + hint + "no rows"/"no search results" + clear button
- L128-277 FULL template: SearchBar + hint + rows counter + spinner + CSV/JSON + table + load-more button
- L280-285 `textWidth` helper — pure, module-level, used only in colWidths

### Consumers (2)
- `grid-view.tsx:90-105` — full props (14, includes `selectedTable=""`)
- `pivot-view.tsx:199-204` — minimal 4 props (`columns`/`rows`/`schema`/`selectedTable`)

### Test surface
- No `data-grid.test.ts` exists. Test surface = 0. Safe extraction (no test update), but no regression net beyond tsc+lint+manual smoke.

## 2. Grill

### Q1: pure function vs hook for colWidths?
**Pure function**. `colWidths` has zero component-state deps — only `columns`/`rows`/`gridColumns` args. `useMemo` in component still needed (memoize on args), but the COMPUTATION extracts to pure `computeColumnWidths(columns, rows, gridColumns)`. Purer, testable, reusable.

### Q2: file location for extracted utils?
`src/lib/data-grid-utils.ts` (new). Matches project convention (`src/lib/*` for pure logic, e.g. `column-type-classifier.ts`, `pivot-config-constants.ts` from Loop A). NOT `src/hooks/` (no React). Contains `computeColumnWidths` + `textWidth` (helper stays co-located, only used there).

### Q3: SearchBar block — subcomponent or inline?
**Subcomponent** `DataGridToolbar` (or `GridSearchBar`). 2 sites differ only in surrounding container (empty template wraps in `flex flex-col h-full`, full in `flex flex-col h-full gap-2`). The SearchBar+hint block itself is identical (6 lines). Extract to local component in same file (not exported, single consumer file).

### Q4: extract EmptyState for M-4?
**SKIP**. Empty template (L103-126) is already clean — early return, no shared logic with full template beyond SearchBar (handled by Q3). Extracting `EmptyState` adds a component for 1 call site, no dedup. Low value.

### Q5: dead `selectedTable` prop — delete?
**YES**. Never destructured, never used in body. Delete from `DataGridProps` interface + 2 call sites (`grid-view.tsx:94`, `pivot-view.tsx:203`). Mechanical.

### Q6: oracle review needed?
**NO**. Scope is mechanical (1 pure function extract + 1 subcomponent extract + 1 dead prop delete). No architectural decision, no store split, no cross-file coupling. Direct plan + fixer.

## 3. Plan

3 phases, sequential, single fixer lane (all touch `data-grid.tsx`):

### P1: extract `computeColumnWidths` + `textWidth` to `src/lib/data-grid-utils.ts` (C-3)
- New file `src/lib/data-grid-utils.ts`: export `computeColumnWidths(columns: string[], rows: unknown[][], gridColumns: {name,type?,comment?}[]): number[]` + export `textWidth(s: string): number`
- `data-grid.tsx`: import `computeColumnWidths`/`textWidth`, replace L57-76 useMemo body with `computeColumnWidths(columns, rows, gridColumns)`, delete L280-285 `textWidth`
- Verify: tsc 0, lint 0, test 250/0

### P2: extract `DataGridToolbar` subcomponent (S-4)
- In `data-grid.tsx`, add local component `DataGridToolbar({searchQuery, onSearchChange, _t})` returning the SearchBar + optional hint block
- Replace L107-112 + L130-134 with `<DataGridToolbar searchQuery={searchQuery} onSearchChange={onSearchChange} _t={_t} />`
- Verify: tsc 0, lint 0, test 250/0

### P3: delete dead `selectedTable` prop
- `data-grid.tsx`: delete `selectedTable: string` from `DataGridProps` interface L15
- `grid-view.tsx:94`: delete `selectedTable={selectedTable ?? ""}`
- `pivot-view.tsx:203`: delete `selectedTable={selectedTable}`
- Verify: tsc 0, lint 0, test 250/0

### M-4 SKIP
Empty/full template split — early return already clean, no extraction value.

## 4. Implementation (DONE — @fixer `ses_0c5b1d04fffelGaNqwEsNwHhf7`)

3 phases sequential, all green:

| Phase | File | Δ | Change |
|-------|------|---|--------|
| P1 | `src/lib/data-grid-utils.ts` | 47 NEW | `textWidth` + `computeColumnWidths` extracted |
| P1 | `src/components/data-grid.tsx` | 286→262 | Replace inline colWidths with `computeColumnWidths()` call, delete local `textWidth` |
| P2 | `src/components/data-grid.tsx` | 262→252 | Add `DataGridToolbar` subcomponent, replace 2 inline SearchBar blocks |
| P3 | `src/components/data-grid.tsx` | 252→251 | Delete `selectedTable` from `DataGridProps` |
| P3 | `src/components/grid-view.tsx` | 117→116 | Remove `selectedTable={selectedTable ?? ""}` |
| P3 | `src/components/pivot-view.tsx` | 209→207 | Remove `selectedTable={selectedTable}` |

`formatType` still used in column header rendering (not dead). `SearchBar` import kept (used by `DataGridToolbar`).

## 5. Verify (DONE)

| Phase | tsc | lint | test |
|-------|-----|------|------|
| P1 | 0 err | 0 err/31 warn | 250/0 |
| P2 | 0 err | 0 err/31 warn | 250/0 |
| P3 | 0 err | 0 err/32 warn | 250/0 |
| **Final** | **0 err** | **0 err/32 warn** | **250/0** |

Final line counts: `data-grid.tsx` 286→251 (-35), `data-grid-utils.ts` 47 (NEW), `grid-view.tsx` 117→116 (-1), `pivot-view.tsx` 209→207 (-2).

### Result
- **C-3**: `colWidths` heuristic (20L useMemo + 6L `textWidth`) → pure `computeColumnWidths` + `textWidth` in `src/lib/data-grid-utils.ts`. Testable, reusable.
- **S-4**: 2× duplicate SearchBar+hint block (6L × 2) → 1 local `DataGridToolbar` subcomponent.
- **M-4**: SKIP (early return already clean, no extraction value).
- **NEW dead prop**: `selectedTable` deleted from interface + 2 call sites.
- **L-4** claim STALE: only 2 consumers (was claimed 3), different prop shapes, no leverage.
