# Improve Loop 14 — M-5: pivot-view.tsx DrilldownDrawer cohabitation

- **Scope**: `src/components/pivot-view.tsx` (203L) — extract `DrilldownDrawer` to own file + fix 2 bugs
- **Pattern**: component extraction (matches Loop A/B pattern) + i18n bug + dead prop
- **Rule sources**: `AGENTS.md` (stack), `~/.config/opencode/memory/core.md` (minimal diff, no `as any`, never commit)
- **Reusable sessions**: none dispatched (mechanical single-file extraction, direct edit)

## 1. Candidate (from Loop 1 explorer)

Original: "M-5 `pivot-view.tsx` DrilldownDrawer cohabitation (S-3)".

Re-grilled after reading pivot-view.tsx (203L post-Loop 12): PivotView mixes 3 concerns — (1) pivot DnD orchestration (DndContext/sensors/handlers/DragOverlay), (2) PivotConfigPanel+PivotGrid layout, (3) DrilldownDrawer (45L, 158-202). DrilldownDrawer already a separate function component, just cohabits same file.

## 2. Grill

### Bugs found (2)
1. **i18n leak** (HIGH): `pivot-view.tsx:177` `{data.rows.length} 行` — hardcoded Chinese. EN mode shows "N 行" while rest of UI is English. `pivot.result_rows` i18n key exists (`{ zh: "行", en: "rows" }` at `i18n.ts:139`). Same pattern as Loop 13 `history-panel.tsx` lang bug.

2. **Dead prop** (MED): `DrilldownDrawer` interface has `selectedTable: string` (line 166), destructured at line 162, but NEVER used in body. Dead prop, passed at call site (line 136 `selectedTable={selectedTable}`). Delete from interface + call site.

### Extraction value
`DrilldownDrawer` (45L) is self-contained: takes `data`/`schema`/`onClose`, renders header + DataGrid. Already a clean function component, just needs its own file. Extraction benefits:
- pivot-view.tsx focuses on pivot DnD + layout (158→~113L)
- DrilldownDrawer reusable (currently 1 consumer, but clean separation)
- Matches Loop A extraction pattern

### Not fixing (out of scope)
- DrilldownDrawer hardcoded `max-h-[40%]` — layout constant, not a bug
- No empty state for drilldown (always has data or isLoading)

## 3. Plan

Single phase, direct edit (no specialist — <50 lines mechanical, 2 files):

- **P1**: Create `src/components/drilldown-drawer.tsx` (45L) — move `DrilldownDrawer` function + imports (`useLang`, `DataGrid`, `ColumnMeta` type)
- **P2**: `pivot-view.tsx` — delete `DrilldownDrawer` function (158-202), delete `DataGrid` import (now only used by DrilldownDrawer), delete `useLang` import (now only used by DrilldownDrawer), delete `ColumnMeta` from type import (now only `TableRef` used). Import `DrilldownDrawer` from new file.
- **P3**: Fix i18n bug — `{data.rows.length} 行` → `{data.rows.length} {_t("pivot.result_rows")}`
- **P4**: Delete dead `selectedTable` prop from `DrilldownDrawer` interface + call site (line 136)

## 4. Implementation (pending — direct edit)

## 5. Verify (pending)

- `npx tsc --noEmit`: 0 errors
- `npm run lint`: 0 errors (≤38 warnings baseline)
- `npm test`: 250 pass / 0 fail
