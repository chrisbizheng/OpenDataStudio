# Improve Loop 23 — C-10 storedConversation dep churn + C-9 assessment

- **Scope**: C-10 (drop `storedConversation` subscription from hydration effect deps) + C-9 assessment (skip)
- **Rule sources**: `AGENTS.md`, `~/.config/opencode/memory/core.md`
- **Reusable sessions**: none (mechanical, <10L, 1 file)

## 1. C-9 assessment (SKIP)

`useAgentChatsStore.setConversation` spread `{ ...s.conversations, [key]: messages }` + `persist` serializes ALL conversations per write. Guard at `use-message-history.ts:59` already skips during streaming (`isLoading` check). Residual cost = O(N conversations) JSON serialization per conversation-completion (not per-token). Realistic N (dozens of tables × short conversations) = <1ms. Not a real perf problem. Debounced persist would add complexity for negligible benefit. **SKIP.**

## 2. C-10 fix (DONE)

**Problem**: `storedConversation = useAgentChatsStore((s) => s.conversations[chatKey])` (l:21) subscribed to persisted store. In hydration effect deps (l:56). Every persist write → `storedConversation` ref changes → effect re-fires → `prevChatKeyRef` short-circuits at l:50. Wasteful re-renders + effect callback churn on every message change.

**Fix**: Drop subscription (l:21 deleted). Read via `useAgentChatsStore.getState().conversations[chatKey]` inside effect body (2 sites: initial hydrate l:45 + chatKey-change l:53). Drop `storedConversation` from deps array. `persist` with `localStorage` hydrates synchronously — `getState()` safe on first render.

**Files**: `src/hooks/use-message-history.ts` — deleted l:21 subscription, 2× `storedConversation` → `const stored = useAgentChatsStore.getState().conversations[chatKey]`, deps array `storedConversation` removed.

## 3. Verify (DONE)

- tsc: 0 errors ✓
- lint: 0 errors, 32 warnings (baseline held) ✓
- npm test: 293 pass / 0 fail ✓

## 4. Result

1 unnecessary subscription eliminated. Hydration effect no longer re-fires on every message persist. C-9 (debounced persist) skipped — guard already mitigates, residual cost negligible.

## Loop closed.
