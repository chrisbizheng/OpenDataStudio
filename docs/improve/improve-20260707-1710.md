# Improve Loop 13 — L-3: QueryPanels same-shape lists + history-panel lang bug

- **Scope**: `src/components/query-panels.tsx` (132L, 3 panels) + `src/components/history-panel.tsx` (47L)
- **Pattern**: bug fix + leverage assessment (SKIP extraction)
- **Rule sources**: `AGENTS.md` (stack), `~/.config/opencode/memory/core.md` (minimal diff, never commit)
- **Reusable sessions**: none dispatched (1-line fix, direct edit)

## 1. Candidate (from Loop 1 explorer)

Original: "L-3 QueryPanels/HistoryPanel/SavedPanel/CommunityPanel same-shape lists".

Re-grilled after reading 3 files: `query-panels.tsx` (132L, 3 panels: HistoryPanel/SavedPanel/CommunityPanel), `history-panel.tsx` (47L, shared by query-panels + pivot-config), `format.ts` (18L, `formatTime(ts, lang="zh")`).

## 2. Grill

### Bug found (HIGH)
`history-panel.tsx:38` `formatTime(item.timestamp)` — missing `lang` arg. `lang` is destructured at line 15 (`const { _t, lang } = useLang()`) but NOT passed to `formatTime`. `formatTime` signature: `(ts: number, lang: Lang = "zh")` — defaults to "zh". 

**Impact**: In EN mode, history timestamps show Chinese ("刚刚", "分钟前", "小时前") while rest of UI is English. Affects BOTH consumers:
- `query-panels.tsx:50` (SQL history)
- `pivot-config.tsx:224` (pivot history)

Only 1 call site with bug (`history-panel.tsx:38`), confirmed by grep `formatTime(` without `lang`.

### Leverage extraction (SKIP)
3 panels share ~15L of container/wrapper CSS (`<div className="p-1.5 space-y-1">`, `<div className="px-2 py-1.5 rounded hover:bg-muted cursor-pointer text-xs">`). But item content genuinely different:
- **HistoryPanel**: font-mono title + timestamp + `meta[]` array
- **SavedPanel**: font-medium name + remove button (✕) + timestamp
- **CommunityPanel**: font-medium name + description (truncate)

Extracting `QueryListItem` wrapper (container div + onClick + title) saves ~3L/panel = 9L total, adds ~10L component + interface. Net ~0. Same pattern as M-4/M-6 in previous loops — already idiomatic, extraction negative value. **SKIP**.

### Other inconsistencies (not bugs, not fixing)
- CommunityPanel has no empty state (HistoryPanel + SavedPanel do)
- Count header: HistoryPanel shows count only if `onClear`, SavedPanel/CommunityPanel always
- These are minor UX differences, not bugs. Not normalizing unless asked.

## 3. Plan

Single 1-line fix, direct edit (no specialist — <5 lines, 1 file):

- `history-panel.tsx:38`: `formatTime(item.timestamp)` → `formatTime(item.timestamp, lang)`

## 4. Implementation (DONE — direct edit)

`history-panel.tsx:38`: `formatTime(item.timestamp)` → `formatTime(item.timestamp, lang)`. 1 line.

## 5. Verify (DONE)

- `npx tsc --noEmit`: 0 errors ✓
- `npm run lint`: 0 errors, 38 warnings (was 39, -1 — fix cleaned 1 unused `lang` warning) ✓
- `npm test`: 250 pass / 0 fail ✓

### Result
1 i18n bug fixed. History timestamps now respect lang setting (EN mode shows "just now"/"X minutes ago" instead of hardcoded Chinese). Affects both consumers: SQL history (`query-panels.tsx:50`) + pivot history (`pivot-config.tsx:224`). Leverage extraction SKIPPED — 3 panels' item content genuinely different, extraction net ~0 (M-4/M-6 pattern).
