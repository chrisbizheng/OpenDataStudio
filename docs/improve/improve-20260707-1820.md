# Improve Loop 18 — Tier 2: dead code cleanup

- **Scope**: 5 dead code candidates from exp-1 scan (m0512). 1 already fixed by Loop 17 (C-25), 4 valid.
- **Rule sources**: `AGENTS.md` (stack), `~/.config/opencode/memory/core.md` (minimal diff, never commit)
- **Reusable sessions**: exp-1/ora-1/fix-1 all reconciled (no specialist dispatched — all <10L mechanical direct edits)

## 1. Candidates

| ID | File | Problem | Status |
|----|------|---------|--------|
| C-25 | `use-agent-chat.ts` | `aiFollowUpQuestions`/`isGeneratingFollowUpQuestions` dead exports | **ALREADY FIXED** by Loop 17 C-7 |
| C-26 | `use-message-history.ts:88-99` | 5 dead fields in return: `setMessages`/`setMessageUI`/`chatKey`/`welcomeMessage`/`getMessages` — only `useAgentChat` consumes, uses only `messages`/`messageUI`/`chatRef`/`toggleThinking`/`clearConversation` | FIXED |
| C-27 | `agent-chats.ts:7,17` | `getConversation` dead method — only consumer (`useMessageHistory:21`) uses `s.conversations[chatKey]` direct | FIXED |
| C-29 | `use-stream-chat.ts:179-182` | dead `else` branch — `currentMessages[placeholderIndex]` always defined by construction (l:51), `else` unreachable | FIXED |
| C-30 | `use-message-history.ts:86,98` | `getMessages` dead since P5 (useStreamChat reads store directly via `getState()`) — same as C-26 | FIXED (folded into C-26) |

## 2. Grill

- **Q1**: Any other consumers of `useMessageHistory`? → grep confirmed only `useAgentChat` imports it. Safe to narrow return.
- **Q2**: Any external caller of `getConversation`? → grep confirmed only `agent-chats.ts` declares it. `useMessageHistory:21` uses field access `s.conversations[chatKey]`. Dead method.
- **Q3**: `use-stream-chat.ts` else branch truly unreachable? → `currentMessages = [...updated, { role: "assistant", content: "" }]` (l:51) always inserts at `placeholderIndex = updated.length`. Every `updatePlaceholderMessage` call preserves length. So `currentMessages[placeholderIndex]` always defined. `else` dead.
- **Q4**: Keep `if (last)` guard after removing else? → YES. TypeScript can't prove array access non-undefined (no `noUncheckedIndexedAccess` proof). Guard narrows type, not dead. Minimal diff.
- **Q5**: Keep internal declarations of `setMessages`/`setMessageUI`/`chatKey`/`welcomeMessage`? → YES. All still used internally (effects + callbacks). Just remove from return object.
- **Q6**: Delete `getMessages` declaration? → YES. Only used in return, no internal use. Delete `useCallback` declaration + return field.

## 3. Plan

3 direct edits, no specialist:
- `use-message-history.ts`: remove 5 fields from return + delete `getMessages` useCallback (l:86)
- `agent-chats.ts`: remove `getConversation` from interface (l:7) + impl (l:17), remove `get` from `persist((set, get) =>` since no longer needed
- `use-stream-chat.ts`: remove dead `else` branch (l:179-182)

## 4. Implementation (DONE — direct edit)

Applied 3 edits as planned. `use-message-history.ts` 100→91L (-9), `agent-chats.ts` 41→39L (-2), `use-stream-chat.ts` 196→193L (-3). Net -14L.

## 5. Verify (DONE)

- `npx tsc --noEmit`: 0 errors ✓
- `npm run lint`: 0 errors, 29 warnings (baseline held) ✓
- `npm test`: 250 pass / 0 fail ✓

## 6. Result

4 dead code sites cleaned. 14L removed. Test baseline preserved. No behavior change.

### 18 loop cumulative
| Loop | Scope | Result |
|------|-------|--------|
| A | pivot-config god-component | 476→259L |
| B | prop-drilling | GridView 18 props→0 |
| C | `_t`/`lang` 11 leaf | prop drilling eliminated |
| D | pivot.ts store split | 233L→config+execution+facade |
| E | force-remount+dead code | 6 items closed |
| fixes | 2 pre-existing test fails | 250/0 baseline |
| C-hooks | 4 hooks | `agent-chat.tsx` 不再调 `useLang()` |
| P5 | 3 hooks→store | AssistantMessage self-sub |
| 7 | data-grid cluster | 286→251L |
| 8 | assistant-message IIFE | IIFE→subcomponent |
| 9 | sql-console global `<style>` | 2 files leak eliminated |
| 10 | dashboardsStore narrowing | 3 perf bugs fixed |
| 11 | SQL dialog error handling | 3 bugs fixed |
| 12 | TableRef bundling | 7 sites 3→1 |
| 13 | history-panel lang bug | 1 i18n bug fixed |
| 14 | DrilldownDrawer extraction | pivot-view 203→155L, 2 bugs |
| 15 | SQL dialog i18n leak | 2 bugs fixed |
| 16 | calc-indicator dialog i18n | 2 bugs fixed |
| review | sidebar i18n + unused destructure + unused import | 3 classes, 5 files, -6 lint warnings |
| 17 | Tier 1 structural bugs | 3 HIGH bugs fixed (C-8 + C-7 + C-24), C-9 deferred |
| 18 | Tier 2 dead code | 4 sites cleaned (C-26 + C-27 + C-29 + C-30), C-25 already fixed by Loop 17 |

### Remaining exp-1 candidates
- Tier 3 i18n 9 (C-11~C-19)
- Tier 4 测试 4 (C-31/C-32/C-33/C-37)
- Tier 5 机械 8 (C-2/C-3/C-20/C-22/C-23/C-28/C-38/C-39)
- Tier 6 结构 3 (C-1/C-4/C-40)
- C-9 deferred
