# Improve Loop 21 — Tier 5 mechanical batch

- **Scope**: 8 mechanical candidates from exp-1 scan (C-2/C-3/C-20/C-22/C-23/C-28/C-38/C-39)
- **Pattern**: dead code, prop drilling remnant, `as any` elimination, hook bypass
- **Rule sources**: `AGENTS.md`, `~/.config/opencode/memory/core.md` (no `as any`, minimal diff)
- **Reusable sessions**: none dispatched (all <15L mechanical direct edits)

## 1. Candidates

5 GO, 3 SKIP:
- **C-2** SKIP — false positive. `pivot-config.tsx:92-103` destructure uses local names consistently, no `dialogs.X` vs `localX` mixing.
- **C-3** SKIP — marginal. `DroppableZone`+`Section`+`Tag` only used in `pivot-config-sections.tsx`. Extraction adds indirection without enabling reuse.
- **C-20** GO — `FollowUpSuggestions` receives `_t` + `lang` props. Self-subscribe `useLang()`, drop both.
- **C-22** GO — `useHistorySelect` custom hook used only 1×. Inline as `useCallback`.
- **C-23** GO — `handleExecute` 1-line `async () => { await executePivot() }` wrapper. Pass `executePivot` directly.
- **C-28** GO — `dashboard-canvas.tsx` `useDashboardsStore.getState().setActiveDashboard(id)` bypasses hook. Add to `useShallow` selector.
- **C-38** GO — `chart-helpers.ts:227` `series as any` (only `as any` in codebase). Replace with `as unknown as EChartsOption["series"]`.
- **C-39** SKIP — `msg.columns!.map(...)` pragmatic `!` for TS closure narrowing. Proper fix = sub-component extraction, not worth it.

## 2. Implementation (DONE — direct edits)

### C-28 `dashboard-canvas.tsx`
Added `setActiveDashboard: s.setActiveDashboard` to `useShallow` selector + destructure. `handleCreateDashboard` uses hook action directly.

### C-22+C-23 `pivot-config.tsx`
- Deleted `useHistorySelect` function (was l:25-37, 13L).
- Inlined callback: `const handleHistorySelect = useCallback((item) => {...}, [dbEntries])`.
- Deleted `handleExecute` wrapper (was l:150-152).
- Changed `onExecute={handleExecute}` → `onExecute={executePivot}`.
- Removed unused `PivotConfig` from type import (was used only by deleted hook's param type).

### C-20 `assistant-message.tsx`
`FollowUpSuggestions` self-subscribes `const { _t, lang } = useLang()`. Dropped `lang` + `_t` from props interface + call site. Matches Cluster C-hooks pattern.

### C-38 `chart-helpers.ts`
`series as any` → `series as unknown as EChartsOption["series"]`. Deleted `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment. `EChartsOption` already imported at l:1. `as unknown as` needed because `ChartSeries[]` (custom union) doesn't structurally overlap with ECharts `SeriesOption[]` (TS would reject direct cast).

## 3. Verify (DONE)

- `npx tsc --noEmit`: 0 errors ✓
- `npm run lint`: 0 errors, 32 warnings (baseline held) ✓
- `npm test`: 293 pass / 0 fail ✓

## 4. Result

5 fixes: 1 hook bypass (C-28), 2 dead code (C-22+C-23, -16L), 1 prop drilling (C-20, 2 props eliminated), 1 `as any` eliminated (C-38, last in codebase). `pivot-config.tsx` 250→237L (-13L). Only `as any` in codebase now gone.

## Loop closed.
