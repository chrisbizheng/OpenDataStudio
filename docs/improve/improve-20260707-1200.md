# Improve Loop — Cluster C-hooks: `_t`/`lang`/`welcomeContent` prop drilling at hooks layer

- **Date**: 2026-07-07
- **Scope**: 4 hooks + 1 component in agent-chat subtree
- **Pattern**: Same as Cluster C (leaf components) but at hooks layer — 3 leaf hooks self-subscribe `useLang()`, orchestrator drops pass-through params
- **Specialists**: skipped explorer (scope fully mapped from reading) + skipped oracle (proven pattern from Cluster C, low risk, no architectural decision)
- **Rule sources**: `AGENTS.md` (stack), `CONTEXT.md` (domain), `~/.config/opencode/memory/core.md` (minimal diff, strict TS)
- **Test baseline**: 250 pass / 0 fail (clean, first fully-green baseline)

## 1. Candidates

Single cluster surfaced during Loop E X-5 re-exam: `welcomeContent`/`lang`/`_t` prop drilling through 4 HOOKS (`useAgentChat`→`useMessageHistory`/`useStreamChat`/`useAiSuggestions`), same pattern eliminated for 11 leaf components in Cluster C.

## 2. Grill

Read 5 anchor files. Prop-drilling map:

```
agent-chat.tsx (useLang → _t, lang)
  └─ welcomeContent = _t("agent.welcome")   [l:20, derived]
  └─ useAgentChat({ lang, welcomeContent, _t, ... })   [l:28-30, PURE FORWARD]
       ├─ useMessageHistory({ welcomeContent, ... })   [l:30]
       ├─ useAiSuggestions({ lang, ... })             [l:37]
       └─ useStreamChat({ lang, _t, ... })             [l:40-51]
```

**Key findings**:
- `useAgentChat` (91L) is PURE PASS-THROUGH for `lang`/`welcomeContent`/`_t` — never uses any directly, only forwards to 3 leaf hooks.
- `agent-chat.tsx` (58L) derives `welcomeContent` + subscribes `useLang()` ONLY to forward to `useAgentChat`. After refactor, `useLang()` call deletable (Cluster C already made Header/InputBar/etc. self-subscribe).
- `useLang()` at `lang-provider.tsx:19-21` returns memoized value (`_t` `useCallback([lang])` l:31, `value` `useMemo` l:33). No re-render storm.
- 3 leaf hooks are custom hooks → calling `useLang()` (another hook) is idiomatic, no rules-of-hooks violation.
- Consumer count: `useAgentChat` 1 (`agent-chat.tsx`); each leaf hook 1 (`useAgentChat`). Clean tree.

**useCallback dep concerns** (verified safe):
- `use-stream-chat.ts:193` `sendMessage` deps include `lang`,`_t` — after refactor both from `useLang()`, identities still change on lang change → deps remain correct.
- `use-message-history.ts:62` effect dep `[messages, chatKey, welcomeMessage.content, setStoredConversation]` — `welcomeMessage` stays `useMemo([welcomeContent])`, `welcomeContent` from `_t("agent.welcome")`. Guarded by `if (isInitialOnly) return`.
- `use-ai-suggestions.ts:136,162` `lang` in 2 useCallback deps — fine.

## 3. Plan

5 phases, all single-file, sequential (file-overlapping via `useAgentChat` orchestrator):

| Phase | File | Action |
|-------|------|--------|
| P1 | `use-message-history.ts` | Add `useLang()`, compute `welcomeContent = _t("agent.welcome")` internally, drop `welcomeContent` param |
| P2 | `use-ai-suggestions.ts` | Add `useLang()` for `lang`, drop `lang` param |
| P3 | `use-stream-chat.ts` | Add `useLang()` for `lang`+`_t`, drop both params |
| P4 | `use-agent-chat.ts` | Drop `lang`/`welcomeContent`/`_t` from params + stop forwarding to 3 leaf hooks |
| P5 | `agent-chat.tsx` | Drop `useLang()` call + `welcomeContent` derivation + 3 props from `useAgentChat()` call |

**TDD**: red→green = `npx tsc --noEmit` fail→pass + `npm run lint` fail→pass + `npm test` (250/0 must hold). Each phase green before next.

## 4. Implementation (DONE — @fixer `ses_0c5b1d04fffelGaNqwEsNwHhf7` reused)

5 phases sequential, all green:

| Phase | File | Δ Lines | tsc | lint | test |
|-------|------|---------|-----|------|------|
| P1 | `use-message-history.ts` | 102→105 (+3) | 0 | 0 err/28 warn | 250/0 |
| P2 | `use-ai-suggestions.ts` | 179→181 (+2) | 0 | 0 err/28 warn | 250/0 |
| P3 | `use-stream-chat.ts` | 200→202 (+2) | 0 | 0 err/28 warn | 250/0 |
| P4 | `use-agent-chat.ts` | 91→84 (-7) | 0 | 0 err/28 warn | 250/0 |
| P5 | `agent-chat.tsx` | 58→54 (-4) | 0 | 0 err/28 warn | 250/0 |

### Changes per phase
- **P1** `use-message-history.ts`: `useLang()` self-subscribe, `welcomeContent` self-derived via `_t("agent.welcome")`, dropped from interface+params. `welcomeMessage` `useMemo([welcomeContent])` intact.
- **P2** `use-ai-suggestions.ts`: `useLang()` self-subscribe `lang`, dropped from interface+params.
- **P3** `use-stream-chat.ts`: `useLang()` self-subscribe `lang`+`_t`, dropped both from interface+params. `sendMessage` deps array already named `lang`/`_t` — unchanged after refactor (now references locals from `useLang()`).
- **P4** `use-agent-chat.ts`: Dropped `lang`/`welcomeContent`/`_t` from interface+destructure+3 child hook calls. PURE pass-through orchestrator no longer forwards.
- **P5** `agent-chat.tsx`: Removed `useLang()` import+call, `welcomeContent` derivation, 3 props from `useAgentChat()` call. Only `tableName`/`schema`/`selectedDatabase`/`onSqlGenerated` remain.

### Surprises
None. All 3 leaf hooks used `lang`/`_t` as declared — no dead params discovered. `sendMessage` deps array already named `lang`/`_t` by name — referenced locals after refactor.

## 5. Verify (DONE)
- tsc --noEmit: 0 errors ✓
- npm run lint: 0 errors, 28 warnings (≤ baseline) ✓
- npm test: 250 pass / 0 fail ✓
- Test baseline preserved (first fully-green baseline held)

### Result
`agent-chat.tsx` no longer calls `useLang()` at all — 3 leaf hooks (`useMessageHistory`/`useAiSuggestions`/`useStreamChat`) self-subscribe `useLang()`, `useAgentChat` orchestrator drops 3 pass-through params. Same pattern as Cluster C (11 leaf components) extended to hooks layer. 5 files, +3/+2/+2/-7/-4 = net -4 lines, prop-drilling surface eliminated across 4 hooks.
